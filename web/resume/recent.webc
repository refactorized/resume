---
layout: shell.webc
jobTitle: Full Stack Web Architect
---

<section class="summary">
  <h2 @text="resume.summary.headline"></h2>
  <p webc:for="para of resume.summary.paragraphs" @text="para"></p>
</section>

<section class="skills">
  <h2>Skills and Experience Across the Entire Stack</h2>
  <ul class="skills-list">
    <li webc:for="g of resume.skills.filter(g => !g.subtle)" @html="g.items.join(', ')"></li>
    <li class="subtle" webc:for="g of resume.skills.filter(g => g.subtle)" @html="g.items.join(', ')"></li>
  </ul>
</section>

<section class="highlights">
  <h2>Career Highlights</h2>
  <div @html="bullets(resume.highlights)"></div>
</section>

<section class="history">
  <h2>Recent Experience</h2>
  <div
    class="job"
    webc:for="job of resume.work.filter(j => j.emphasis !== 'omit' && recencyYear(j) >= 2020)"
  >
    <div @html="jobHeader(job)"></div>
    <div @html="bullets(job.highlights)"></div>
  </div>
</section>

<section class="history additional">
  <h2>Additional Experience</h2>
  <div
    class="job"
    webc:for="job of resume.work.filter(j => j.emphasis !== 'omit' && recencyYear(j) < 2020 && recencyYear(j) >= 2014)"
  >
    <div @html="jobHeader(job)"></div>
  </div>
  <p class="earlier">
    <em>Earlier:</em>
    <span
      @text="resume.work.filter(j => recencyYear(j) < 2014).map(j => j.company).join(' · ')"
    ></span>
  </p>
</section>

<section class="education">
  <h2>Education</h2>
  <ul>
    <li
      webc:for="e of resume.education"
      @html="e.credential + ' <em>from</em> ' + e.institution + ' <em>in</em> ' + e.year"
    ></li>
    <li
      @html="'Certifications: ' + resume.certifications.map(g => g.names.join(', ') + ' <em>in</em> ' + g.year).join(', ')"
    ></li>
  </ul>
</section>
